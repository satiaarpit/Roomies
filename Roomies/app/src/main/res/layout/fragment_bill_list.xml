<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:fab="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_height="wrap_content" tools:context="com.example.asatia.roomies.BillList">

    <RelativeLayout
        style="@style/mainLayout"
        android:id="@+id/billListMain">
        <TextView
            android:text="Bills"
            style="@style/titleStyle"
            />
    </RelativeLayout>

    <ListView
        android:id="@+id/bill"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:dividerHeight="1dp"
        android:divider="@color/basicColor"
        android:layout_below="@id/billListMain"
        android:layout_marginTop="40dp"
        android:layout_alignParentBottom="true"
        >
        </ListView>
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/floatingButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="12dp"
        app:backgroundTint="@color/buttonColor"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:src="@drawable/add"
       />
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/settleButton"
        style="@style/okButton"
        app:backgroundTint="@color/buttonColor"
        />
</RelativeLayout>
